import React from 'react';
import styles from './Hero.module.css';

const Hero = () => {
  return (
    <section className={styles.hero}>
      <div className={styles.container}>
        <h1>Hero Section - В разработке</h1>
      </div>
    </section>
  );
};

export default Hero;
